const User = require("../models/User");


exports.GetBooks = (request, response, next) => {
  //verificar si el usuário está logueado


  /*  return response.status(200).render("books", {
    pageTitle: "Books",
  }); */
};
